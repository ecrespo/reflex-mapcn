"""Clients for the public services the demo reads from.

Nothing here belongs to the published package: these live in the demo app,
call out with explicit timeouts, cache what they get, and always return a
value the page can render, error included.
"""
